# AsterEngine
Aster Engine is a lightweight 2D software rendering engine written in C, using a custom framebuffer and manual rasterization. Built with Win32, it renders everything without relying on hardware APIs like OpenGL or DirectX. The project focuses on understanding low-level graphics and how a rendering pipeline works from scratch.
